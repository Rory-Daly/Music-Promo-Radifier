// Vitest stub for the `server-only` package. The real package throws when
// imported outside a React Server Component bundle; under tests we want a
// no-op so server-marked modules can be exercised in Node.
export {}
